import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    registerContainer:{
        paddingTop:'2%',
        flexDirection:'row',
        alignItems:'center',
      },
      tandc:{
        marginTop:8,
        flexDirection:'column',
        alignItems:'center',
      }
});